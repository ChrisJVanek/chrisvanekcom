# Optional: required for POST /api/health/ingest (Apple Health sync, Shortcuts, etc.)
# HEALTH_INGEST_SECRET=your-secret-token

# Required for Cronometer sync (scripts/cronometer-sync.js and GitHub Action)
# CRONOMETER_EMAIL=your@email.com
# CRONOMETER_PASSWORD=your-password
